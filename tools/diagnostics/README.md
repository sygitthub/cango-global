# Diagnostics

Small helper scripts used to inspect generated CANGO data.

These files are not part of the live website. The dashboard can run without
them. Keep primary update scripts in the project root:

- `build_site_data.py`
- `rebuild_cango_from_excel.py`
- `json_to_js_lite.py`

